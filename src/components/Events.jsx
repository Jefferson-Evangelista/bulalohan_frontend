import React from 'react'
import styles from '../style'

const Events = () => {
  return (
    <section id='events' className={`${styles.marginY} flex justify-start items-center container max-w-full`}>
        <h1 className='font-cormorant font-bold text-[60px]'>Events <span className='text-title'>& Services</span></h1>
    </section>
  )
}

export default Events